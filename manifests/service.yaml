apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-argo-demo
spec:
  selector:
    app: nginx-argo-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort 
